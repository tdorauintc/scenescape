# SPDX-FileCopyrightText: (C) 2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-datasets-pvc
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "{{ int .Values.pvc.hookWeight }}"
spec:
  storageClassName: {{ .Values.datasets.storageClassName }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.datasets.storage }}

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-netvlad-pvc
spec:
  {{- if .Values.autocalibration.modelStorageClassName }}
  storageClassName: {{ .Values.autocalibration.modelStorageClassName | quote }}
  {{- end }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.autocalibration.modelStorage }}
